__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Testing
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
# Orphaned temp dirs with broken ACLs (undeletable without elevation);
# ignoring them silences git's could-not-open-directory warnings.
.pytest_tmp/
tests/_tmp_pytest/
tmpvb4qwmfq/

# Environment
.env

# KiCad MCP
.kicad_mcp_backups/
*.jsonl
# ChangeLog runtime artifact (written by tests/tools; never a source file)
changes.json

# Examples (local testing only)
examples/

# OS
Thumbs.db
.DS_Store

# Never push — internal dev notes and machine-local config
LESSONS_LEARNED.md
MCP-KNOWN-ISSUES.md
CLAUDE.md
**/CLAUDE.md
.claude/
logs/
null
kicad-dev-docs/
docs/specs/

# Developer-local scratch board used as a throwaway target for the
# integration test suite (tests/integration/). Never checked in.
tests/_scratch/

# Runtime diagnostic log the bridge writes next to its module file —
# created in-repo when tests import kicad_plugin/kicad_mcp_bridge.py.
kicad_plugin/bridge_startup.log
